import styled from "styled-components";
import { StyledLabel } from "../styled/SharedStyles.styled";

export const MarginedStyledLabel = styled(StyledLabel)`
    margin: 0 10px;
`

export const ConfirmationLabelWrapper = styled.div`
        margin: 20px 0;
`